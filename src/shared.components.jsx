

// abs positioned link back to home page
export const HomeLink = ({text
}) => {
    return (
    <div className="absolute top-0 inset-1">
        <a href="/" className="font-bold">Home</a>
    </div>
    )
}